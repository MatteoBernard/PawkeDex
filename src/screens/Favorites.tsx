import {View, Text} from "react-native";
import {Template} from "./Template";

export const Favorites = () => {
    return (
        <Template>
            <Text>Favorites</Text>
        </Template>
    )
}